# Archived prototype

This directory preserves the original prototype's module structure as a design reference only.
All fictional business data, generated content, obsolete pages, and prototype runtime state were removed.
The production application lives at the repository root.
