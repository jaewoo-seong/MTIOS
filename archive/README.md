# Archived product surfaces

This directory preserves unused pre-research-product code for reference. It is excluded from TypeScript compilation and deployment build contexts and must not be imported by the application.

The first archived components were disconnected before this move and had no application imports. Legacy API and database modules remain in place until their data ownership and migration paths are verified; moving them without that work would risk breaking persisted workflows.
